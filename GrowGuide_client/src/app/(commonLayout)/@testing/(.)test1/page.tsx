import React from "react";
import CustomModal from "../../../UI/customModal/CustomModal";
const page = () => {
  return (
    <div>
      modal custom
      <CustomModal>intercepter pgae</CustomModal>
    </div>
  );
};

export default page;
